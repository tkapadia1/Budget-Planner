import 'package:flutter/material.dart';

class MyDeletionPage extends StatelessWidget {
  final String transactionName;

  MyDeletionPage({
    required this.transactionName,
  });

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
